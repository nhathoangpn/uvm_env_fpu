1. Supported Functions:

   a.Floating point functions:
     f32_add
     f32_sub
     f32_mul
     f32_mulAdd
     f32_mulSub
     f32_mulNegateAdd
     f32_mulNegateSub
     f32_div
     f32_sqrt
     f32_min
     f32_max
     f32_eq
     f32_lt
     f32_le
     f32_ne
     f32_to_i32
     f32_to_ui32
     i32_to_f32
     ui32_to_f32
     f32_exp2
     f32_log2

   b.Integer functions:
     i32_add
     ui32_add
     i32_sub
     ui32_sub
     i32_mul
     i32_mulh
     ui32_mul
     ui32_mulh
     i32_ui32_mul
     i32_ui32_mulh
     i32_mulAdd
     i32_mulNegateAdd
     i32_mulSub
     i32_mulNegateSub
     i32_div
     ui32_div
     i32_rem
     ui32_rem

2. How to compile:

   . cd Lib/TestFloat-3e/build/Linux-riscV-GCC
   . source compile

3. Run and options:

   * Run command for floating point should be: testfloat_ver -checkNaNs -tininessbefore <rounding mode> <others option> <function>

   * Run command for integer should be       : testfloat_ver  <function>

   
  <option>:  (* is default)
  Common Option:
      -help            --Write this message and exit.
      -errors <num>    --Stop after <num> errors.
   *  -errors 20
  
  NaN option:
      -checkNaNs       --Check for specific NaN results.
  
  Rounding Mode option (used for f32_add, f32_sub, f32_mul, f32_mulAdd, f32_mulSub, f32_mulNegateAdd, f32_mulNegateSub, f32_div, f32_sqrt):
   *  -rnear_even      --Round to nearest/even.
      -rminMag         --Round to minimum magnitude (toward zero).
      -rmin            --Round to minimum (down).
      -rmax            --Round to maximum (up).
      -rnear_maxMag    --Round to nearest/maximum magnitude (nearest/away).
      -rodd            --Round to odd (jamming).  (For rounding to an integer
                           value, rounds to minimum magnitude instead.)
  
  Tininess option (only used for f32_mul, f32_mulAdd, f32_mulSub, f32_mulNegateAdd, f32_mulNegateSub):
      -tininessbefore  --Detect underflow tininess before rounding.
   *  -tininessafter   --Detect underflow tininess after rounding.

  Flush Mode option (Disable by default):
      -flush           --Enable Subnormal Flush Mode check (fn = 1) .

  **Note:  1. When using option for not support function, option will be ignored (Ex: Using rounding option for integer function is meaningless and will be ignored).
           2. Option -checkNaNs is needed every time when checking for floating point function.
           3. -tininessbefore should be used when checking for support functions above.

4. Error types:
   
   a. Invalid argument                                      : Wrong option name, function name
   b. Invalid input format                                  : Wrong format of input operands
   c. Errors found in <function>                            : Mismatched between result of input and expected result when checking for <function> is found
   d. Error line: <number>                                  : Error is found in line <number> of input file
   e. => <result> ......  expected <expected result> ...... : <result> in input file is mismatched with <expected result> generated by model


5. SVN Rev: 304
